# macOS
.DS_Store

# Xcode
*.xcuserstate
xcuserdata/

# E2E secrets — never commit (copy from TestSecrets.swift.example, then fill in)
/SampleAppUITests/Helpers/TestSecrets.swift

# Local test plans hold E2E secrets — never commit
*.local.xctestplan

# CocoaPods
Podfile
Podfile.lock
Pods/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Build Artifacts
build/
DerivedData/
*.app
*.ipa
*.dSYM

# Code Coverage
*.xccoverage

# Downloaded xcframeworks (local dev only)
CometChatUIKitSwift/CometChatUIKitSwift.xcframework/

# Repo snapshot archives (local dev only)
*.zip
